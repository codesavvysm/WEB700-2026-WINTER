# Class Exercises - Week 5

## Exercise: Express Web Server & Deploy to Vercel

Create a web server using Express with two routes:

- **GET /** — responds with `"web site root"`
- **GET /about** — responds with `"created by <your name>"` (use your actual name)

Deploy the server to Vercel.
